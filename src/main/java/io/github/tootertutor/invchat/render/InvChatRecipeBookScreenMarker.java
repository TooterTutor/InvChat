package io.github.tootertutor.invchat.render;

/** Marker added to recipe-book container screens so the generic render hook can avoid double-draws. */
public interface InvChatRecipeBookScreenMarker {
}
